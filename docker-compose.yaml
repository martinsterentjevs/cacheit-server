services:
  db:
    image: postgres:15-alpine
    container_name: cacheit-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cacheit
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
